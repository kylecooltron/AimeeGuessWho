{
    "name": "aimee-guess-who-backend",
    "version": "1.0.0",
    "description": "WebSocket backend for Guess Who game",
    "main": "src/server.js",
    "scripts": {
        "start": "node src/server.js",
        "dev": "nodemon src/server.js",
        "kill": "for /f \"tokens=5\" %a in ('netstat -ano ^| findstr :3001') do taskkill /PID %a /F"
    },
    "dependencies": {
        "express": "^4.18.2",
        "ws": "^8.13.0",
        "cors": "^2.8.5",
        "dotenv": "^16.3.1"
    },
    "devDependencies": {
        "nodemon": "^3.0.1"
    }
}
